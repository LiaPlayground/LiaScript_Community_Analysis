% ~600 words

\subsection{Data Collection Pipeline}

\begin{figure}[htbp]
\centering
\resizebox{\columnwidth}{!}{%
\begin{tikzpicture}[
    font=\small,
    node distance=5mm and 5mm,
    >={Stealth[length=2.5mm,width=1.8mm]},
    base/.style={
        rectangle,
        rounded corners=1.8mm,
        draw,
        line width=0.7pt,
        align=center,
        minimum height=1.1cm,
        text width=2.6cm,
        inner sep=3pt,
        font=\footnotesize
    },
    search/.style={base, draw=blue!55!black, fill=blue!8},
    validate/.style={base, draw=orange!70!black, fill=orange!12},
    enrich/.style={base, draw=teal!60!black, fill=teal!10},
    aibox/.style={base, draw=violet!60!black, fill=violet!8},
    finalbox/.style={base, draw=black, fill=gray!12},
    flow/.style={->, line width=0.8pt, draw=black!75},
    split/.style={fill=black, circle, inner sep=1.2pt},
    groupbox/.style={rounded corners=2mm, draw=black!45, dashed,
        line width=0.6pt, inner sep=5pt},
    grouplabel/.style={font=\scriptsize\bfseries, fill=white, inner sep=1.5pt}
]

% --- Vertical start, then horizontal flow ------------------------------------

\node[search] (search) {Repository search\\GitHub Search API\\[0.5mm]
\textbf{1,076 repos}};

\node[search, below=of search] (files) {File extraction\\Tree API + heuristics\\[0.5mm]
\textbf{57,096 files}};

\node[validate, right=of files] (valid) {Validation\\Rules + LLM\\[0.5mm]
\textbf{3,317 courses}\\
\emph{\scriptsize(5.8\,\% of files)}};

% --- Split into three parallel branches ---------------------------------------

\coordinate[right=1mm of valid] (split);

\node[enrich, above right=2mm and 6mm of valid] (header) {Header metadata\\ parsing\\[0.5mm]
\textbf{3,297 courses}\\
\emph{\scriptsize(99.4\,\% coverage)}};

\node[enrich, right=6mm of valid] (features) {Feature detection\\44 regex patterns\\[0.5mm]
\textbf{44 features}};

\node[aibox, below right=2mm and 6mm of valid] (aiclass) {AI classification\\LLM (llama3.3:70b)\\[0.5mm]
\textbf{Education level}\\
\emph{\scriptsize(6 categories)}};

% --- Merge -------------------------------------------------------------------

\node[finalbox, right=38mm of split] (final) {Consolidation\\Analysis\\[0.5mm]
\textbf{2,973 courses}};

% --- Arrows ------------------------------------------------------------------

\draw[flow] (search) -- (files);
\draw[flow] (files) -- (valid);

\draw[flow] (valid.east) -- ++(3mm,0) |- (header.west);
\draw[flow] (valid.east) -- ++(3mm,0) -- (features.west);
\draw[flow] (valid.east) -- ++(3mm,0) |- (aiclass.west);

\coordinate (merge) at ([xshift=-8mm]final.west);
\draw[flow] (header.east) -- ++(3mm,0) |- (merge);
\draw[flow] (features.east) -- (merge);
\draw[flow] (aiclass.east) -- ++(3mm,0) |- (merge);
\draw[flow] (merge) -- (final.west);

% --- Group annotations -------------------------------------------------------

\begin{scope}[on background layer]
    \node[groupbox, fit=(search)(files), inner xsep=8pt, inner ysep=5pt] (g1) {};
    \node[grouplabel, above=1mm of g1.north] {Candidate identification};

    \node[groupbox, fit=(valid)] (g2) {};
    \node[grouplabel, above=1mm of g2.north] {Course validation};

    \node[groupbox, fit=(header)(features)(aiclass), inner ysep=5pt] (g3) {};
    \node[grouplabel, above=1mm of g3.north] {Feature extraction \& classification};
\end{scope}

\end{tikzpicture}%
}
\caption{Data construction workflow.  The AI classification stage assigns
an education level to each course, enabling the community segmentation
described in Section~\ref{sec:four_groups}.}
\label{fig:data_pipeline_pub}
\end{figure}


The dataset was constructed through a multi-stage pipeline
(\Cref{fig:data_pipeline_pub}), implemented in the open-source
\emph{ConnectedLecturer} project.\footnote{\url{https://github.com/TUBAF-IFI-ConnectedLecturer/Data_aggregation}}
Starting from GitHub's Search API, we identified \num{1076} repositories
containing potential LiaScript content (collected 2026-03-13).
A tree-level aggregation step extracted \num{57096} Markdown files,
of which \num{3317} (\SI{5.8}{\percent}) were validated as LiaScript courses
using a combination of rule-based filters and an LLM classifier.
For each validated course, the pipeline (a)~parsed header metadata
(\SI{99.4}{\percent} success rate), (b)~extracted \num{44} binary
feature flags, and (c)~assigned an education level via an LLM-based
classifier (llama3.3:70b) that categorises each course into one of
six levels (Primary, Secondary~I/II, Higher Education, Vocational,
Continuing Education).

\subsection{Feature Taxonomy}
\label{sec:taxonomy}

For each course file the pipeline extracts \num{44} binary feature flags
via syntactic pattern matching on the Markdown source.\footnote{All regex patterns and their mapping to feature labels are documented at \url{https://github.com/TUBAF-IFI-ConnectedLecturer/Data_aggregation/blob/main/cl-pipeline/run/pipelines/liascript/docs/LiaScript_Features_Patterns.md}}
We organise these features into four didactically motivated categories
(Table~\ref{tab:taxonomy}).

\begin{table}[ht]
  \centering
  \caption{LiaScript feature taxonomy (4 categories, 44 tracked features)}
  \label{tab:taxonomy}
  \begin{tabular}{p{2.0cm}p{3.8cm}p{5.6cm}}
    \toprule
    \textbf{Category} & \textbf{Design intent} & \textbf{Representative features} \\
    \midrule
    \textcolor{catPresentation}{\textbf{Presentation}} &
      Self-paced, accessible delivery &
      Narrator (TTS), animations, effects, galleries \\[4pt]
    \textcolor{catInteraction}{\textbf{Interaction}} &
      Active learning \& formative feedback &
      Single/multiple choice, text input, matrix quiz, surveys \\[4pt]
    \textcolor{catReuse}{\textbf{Reuse}} &
      Scalable authoring via shared components &
      Imports, custom macros, official templates \\[4pt]
    \textcolor{catEmbedding}{\textbf{Embedding}} &
      Hands-on executable environments &
      Executable code, WebApps, external scripts, ASCII diagrams \\
    \bottomrule
  \end{tabular}
\end{table}

Each category addresses a different pedagogical barrier.
\emph{Presentation} features target accessibility (screen-reader-friendly TTS,
animated step-by-step reveals).
\emph{Interaction} features enable formative self-assessment without a learning
management system.
\emph{Reuse} features support the Don't-Repeat-Yourself principle at course level,
allowing shared building blocks across a curriculum.
\emph{Embedding} features close the gap between explanation and practice by
running code, simulations, or visualisations directly in the browser.

\subsection{Four-Group Segmentation}
\label{sec:four_groups}

Each course resides in a GitHub repository owned by an individual or
organisation account.
The \num{3317} courses were contributed by \num{314} unique committers
working across \num{205} repository accounts (\num{167}~individual,
\num{37}~organisational, 1~unclassified).
Organisation accounts act as hosting containers; the actual authors are
identified through the Git commit history (\texttt{contributors\_list}).

To prevent distortions from authors with fundamentally different
relationships to LiaScript, we first separate courses by
\emph{repository context} into Internal and MINT-the-GAP.
Within the remaining community courses, the AI-based education-level
classification reveals that higher-education courses and school-level
courses exhibit substantially different feature profiles
(Section~\ref{sec:profiles}), motivating a further split.
The resulting four groups are:
\begin{description}
  \item[Internal] (462~courses, 7~repository accounts):
    Repositories owned by the core development team and their
    organisational accounts
    (SebastianZug, andre-dietrich, LiaPlayground, LiaScript, LiaBooks,
    LiaTemplates, TUBAF-IfI-LiaScript).
    These include templates, demonstrations, and the developers' own
    teaching materials.
  \item[MINT-the-GAP] (1\,147~courses, 1~organisation account):
    A single prolific school-focused initiative producing structured
    STEM courses.
    With nearly a third of all courses, this account would dominate any
    community-level analysis if not separated.
  \item[Comm-Uni] (1\,178~courses, 167~repository accounts):
    Community courses classified as \emph{Higher Education} by the
    AI classifier.
    This is the largest and most diverse group, spanning universities
    across multiple countries and disciplines.
    The largest single account contributes \SI{10.9}{\percent} of courses;
    excluding single-course accounts shifts adoption rates by less than
    one percentage point.
  \item[Comm-School] (186~courses, 38~repository accounts):
    Community courses classified as \emph{Primary}, \emph{Secondary~I},
    or \emph{Secondary~II}.
\end{description}

\noindent
The remaining \num{344} community courses classified as Vocational
Education, Continuing Education, or without a clear education-level
assignment are excluded from the group comparison, yielding
\num{2973}~courses for the four-group analysis.

Because grouping follows the repository context, the same committer
may appear in multiple groups.
In particular, 22~committers --- including the two core developers ---
contribute to both Internal and Community repositories.
Their work in Community repositories is counted as Community,
reflecting the collaborative context.

\subsection{Limitations}

The corpus is restricted to \emph{public} GitHub repositories.
Courses hosted on other platforms, behind authentication, or in private
repositories are not captured.
Feature detection via pattern matching may miss features used through
indirect mechanisms (e.g.\ macros that generate quizzes programmatically).
The four-group segmentation assigns courses by repository context
and AI-classified education level,
so the 22~committers active in multiple groups are counted in each
group separately rather than as a single entity.
The AI-based education-level classification has not been manually
validated beyond spot-checks against Internal and MINT-the-GAP;
ambiguities between Secondary~I and~II are possible.
The Comm-School group is comparatively small (\num{186}~courses,
\num{38}~accounts) and shows moderate author concentration
(top-3 accounts contribute \SI{44.1}{\percent}).
%, which should be considered when interpreting its adoption rates.
